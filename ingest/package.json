{
  "name": "hermys-ingest",
  "version": "1.0.0",
  "description": "Phase 5 scaffold: segments → draft beats/entity-cards into packs/ (gitignored), schema-validated, human-approved on the Prep Bench",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "ingest": "tsx src/cli.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
